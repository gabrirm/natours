#popup.popup 
    .popup__content
        if user
            a.nav__el.nav__el--logout.popup__item.popup__item--logout Log out
            a.nav__el(href='/me')
                    img.nav__user-img(src=`/img/users/${user.photo}`, alt=`Photo of ${user.name}`)
                    span= user.name.split(' ')[0]
        else
            a.nav__el.popup__item(href='/login') Log in 
            a.nav__el.nav__el--cta(href='/signup') Sign up